name: CI

on:
  push:
    branches: [master, cache-modified-ci]
  pull_request:
    branches: [master]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5

      # Keep this version in sync with the ruff pin in uv.lock, so CI matches
      # what `ruff` resolves to locally instead of drifting to latest.
      - name: Check formatting
        run: uvx ruff@0.15.22 format --check .

      - name: Run linter
        run: uvx ruff@0.15.22 check .

  test:
    name: Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: metron_test
          POSTGRES_USER: metron
          POSTGRES_PASSWORD: metron
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      SECRET_KEY: "ci-secret-key-not-for-production"
      DEBUG: "True"
      ALLOWED_HOSTS: "localhost,127.0.0.1"
      DB_NAME: metron_test
      DB_USER: metron
      DB_PASSWORD: metron
      DB_HOST: localhost
      REDIS_URL: "redis://localhost:6379"
      THUMBNAIL_REDIS_HOST: localhost
      EMAIL_BACKEND: "django.core.mail.backends.console.EmailBackend"
      EMAIL_HOST: "localhost"
      EMAIL_USER: "test@example.com"
      EMAIL_PASSWORD: "dummy"
      PUSHOVER_TOKEN: "dummy"
      PUSHOVER_USER_KEY: "dummy"
      HCAPTCHA_SECRET_KEY: "dummy"
      RAPID_API_KEY: "dummy"
      RAPID_API_HOST: "dummy"
      OPENCOLLECTIVE_API_KEY: "dummy"
      STATIC_ROOT: "/tmp/static"
      MEDIA_ROOT: "/tmp/media"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --dev

      - name: Run tests
        run: uv run pytest

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: test-results/coverage/
          retention-days: 14
